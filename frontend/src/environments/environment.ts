export const environment = {
    backendHost: '${BACKEND_HOST}',
    backendPort: '${BACKEND_PORT}',
    backendProtocolSchema: '${BACKEND_PROTOCOL_SCHEMA}',
    backendModeratorPort:  '${BACKEND_MODERATOR_PORT}'
};